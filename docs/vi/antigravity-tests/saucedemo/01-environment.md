# Môi trường & dữ liệu test — Sauce Demo

| Mục | Giá trị |
|-----|---------|
| URL | `https://www.saucedemo.com/` |
| User hợp lệ | `standard_user` / `secret_sauce` |
| User bị khóa | `locked_out_user` / `secret_sauce` |
| Sai mật khẩu (TC-02) | `standard_user` / `wrong_password` |

**Lưu ý:** Chỉ dùng tài khoản demo công khai trên site; không nhập mật khẩu thật hay PII.
